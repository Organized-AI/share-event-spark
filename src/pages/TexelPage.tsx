import { TexelIntegration } from '@/components/TexelIntegration';

const TexelPage = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 to-slate-100">
      <TexelIntegration />
    </div>
  );
};

export default TexelPage;